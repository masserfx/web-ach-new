'use client'; import { motion } from 'framer-motion'; import Link from 'next/link'; import { Calendar, Clock, ArrowRight } from 'lucide-react'; interface BlogPost { id: string; slug: string; title: string; excerpt: string; featured_image?: string; published_at: string; reading_time?: number; tags?: string[]; } interface LatestBlogPostsProps { posts: BlogPost[]; } export function LatestBlogPosts({ posts }: LatestBlogPostsProps) { if (posts.length === 0) return null; return ( <section className="container mx-auto px-4 py-16 bg-gradient-to-b from-graphite/40 to-carbon"> {/* Header */} <div className="text-center max-w-3xl mx-auto mb-12"> <h2 className="text-4xl md:text-5xl font-bold mb-4"> <span className="bg-gradient-to-r from-accent to-brand-accent bg-clip-text text-transparent"> Nejnovější články </span> </h2> <p className="text-xl text-steel-dim"> Tipy, rady a novinky ze světa tepelných čerpadel a fotovoltaiky </p> </div> {/* Posts Grid */} <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12"> {posts.map((post, index) => ( <BlogPostCard key={post.id} post={post} index={index} /> ))} </div> {/* View All Link */} <div className="text-center"> <motion.div whileHover={{ scale: 1.05, y: -2 }} whileTap={{ scale: 0.95 }} > <Link href="/blog" className="inline-flex items-center gap-2 px-10 py-5 bg-gradient-to-r from-accent to-accent/90 text-white rounded-xl font-bold text-lg shadow-2xl hover:shadow-primary/30 transition-all" > Zobrazit všechny články <ArrowRight className="" /> </Link> </motion.div> </div> </section> ); } function BlogPostCard({ post, index }: { post: BlogPost; index: number }) { const publishedDate = new Date(post.published_at).toLocaleDateString('cs-CZ', { year: 'numeric', month: 'long', day: 'numeric' }); const gradients = [ 'from-accent/10 to-amber-700/10', 'from-accent-dark/10 to-green-600/10', 'from-brand-accent/10 to-orange-600/10' ]; const gradient = gradients[index % gradients.length]; return ( <motion.article initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.1 }} whileHover={{ y: -8, scale: 1.02 }} className="relative group bg-carbon rounded-2xl overflow-hidden shadow-2xl hover:shadow-3xl transition-all border-2 border-accent/10" > {/* Gradient overlay */} <div className={`absolute top-0 left-0 right-0 h-1 bg-gradient-to-r ${gradient.replace('/10', '')}`} /> {/* Image */} {post.featured_image && ( <Link href={`/blog/${post.slug}`} className="block relative aspect-video overflow-hidden bg-gradient-to-br from-graphite-light to-graphite"> <img src={post.featured_image} alt={post.title} className="" /> {/* Dark overlay on hover */} <div className="" /> </Link> )} {/* Content */} <div className="relative p-6"> {/* Meta */} <div className="flex items-center gap-4 text-sm text-steel-dim mb-3"> <span className="flex items-center gap-1"> <Calendar className="" /> {publishedDate} </span> {post.reading_time && ( <span className="flex items-center gap-1"> <Clock className="" /> {post.reading_time} min </span> )} </div> {/* Title */} <Link href={`/blog/${post.slug}`}> <h3 className="text-xl font-bold mb-3 text-white group-hover:text-accent transition-colors line-clamp-2"> {post.title} </h3> </Link> {/* Excerpt */} <p className="text-steel-dim mb-4 line-clamp-3 leading-relaxed">{post.excerpt}</p> {/* Read More */} <Link href={`/blog/${post.slug}`} className="inline-flex items-center gap-2 text-accent font-bold hover:gap-3 transition-all" > Číst více <ArrowRight className="" /> </Link> {/* Decorative element */} <div className={`absolute -bottom-4 -right-4 w-32 h-32 bg-gradient-to-br ${gradient} rounded-full blur-3xl opacity-0 group-hover:opacity-100 transition-opacity`} /> </div> </motion.article> ); } 